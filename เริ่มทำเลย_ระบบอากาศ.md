# เริ่มทำเลย — ระบบอากาศ

**ทำ 4 ขั้น รวมประมาณ 45 นาที แล้วจบ**
แต่ละขั้นทำเสร็จแล้วใช้งานได้ทันที ไม่ต้องรอขั้นถัดไป

```
ขั้น 1  เอาแอปขึ้น iPhone           15 นาที   → ได้ไอคอนแอปบนมือถือ
ขั้น 2  สร้าง Telegram Bot          10 นาที   → ได้ TOKEN + CHAT_ID
ขั้น 3  ทดสอบบนคอม                  10 นาที   → ได้ข้อความเข้า Telegram
ขั้น 4  ตั้งให้รันเอง 24 ชม.         15 นาที   → เตือนเองไม่ต้องเปิดคอม
```

---
---

# ขั้นที่ 1 — เอาแอปขึ้น iPhone

### 1.1 อัปโหลดขึ้น Netlify

1. เปิด https://app.netlify.com แล้วล็อกอิน
2. กด **Add new site** (หรือ **Add new project**) → เลือก **Deploy manually**
3. เปิด File Explorer ไปที่ `D:\AnthropicClaude\Ai\Cladue\Weather`
4. **ลากโฟลเดอร์ `pwa` ทั้งโฟลเดอร์** เข้าไปวางในกรอบบนหน้าเว็บ Netlify

   > ต้องลากทั้งโฟลเดอร์ ไม่ใช่ลากไฟล์ข้างในทีละไฟล์
   > เพราะโครงสร้างโฟลเดอร์ `icons/` ต้องคงอยู่

5. รอสัก 10-30 วินาที จะได้ URL แบบ `https://random-name-123.netlify.app`

### 1.2 ตั้งชื่อให้จำง่าย (ทำหรือไม่ทำก็ได้)

**Site configuration → Change site name** → พิมพ์ `bangpakong-rain`
จะได้ `https://bangpakong-rain.netlify.app`

### 1.3 ติดตั้งลงหน้าจอ iPhone

1. เปิด URL นั้นด้วย **Safari** บน iPhone

   > ⚠️ ต้องเป็น Safari เท่านั้น Chrome บน iOS ไม่มีเมนูนี้

2. กดปุ่มแชร์ (สี่เหลี่ยมมีลูกศรชี้ขึ้น) ที่แถบล่าง
3. เลื่อนลงหา **"เพิ่มไปยังหน้าจอโฮม"** แล้วกด
4. ตั้งชื่อ (เช่น "เช็คฝน") → กด **เพิ่ม**

### ✅ ขั้นที่ 1 เสร็จเมื่อ

มีไอคอนเมฆฝนสีน้ำเงินบนหน้าจอ iPhone กดแล้วเปิดเต็มจอ ไม่เห็นแถบ Safari

### ถ้าติดปัญหา

| อาการ | แก้ |
|---|---|
| กดปุ่มตำแหน่งปัจจุบันแล้วไม่ขึ้น | Settings → Safari → Location → **Allow** แล้วปิดแอปเปิดใหม่ |
| ภาพดาวเทียมไม่ขึ้น (เรดาร์ขึ้นปกติ) | เป็นเรื่องปกติ ภาพดาวเทียมเป็น http กด "เปิดหน้าเต็ม" ดูแทนได้ |
| ตัวเลขไม่ขึ้นเลย | เช็คว่ามีเน็ต แล้วกดปุ่ม "รีเฟรชทั้งหมด" |
| แก้ไฟล์แล้วอยากอัปเดต | ลากโฟลเดอร์ `pwa` ไปวางที่ Netlify อีกครั้ง (Deploys → Deploy manually) |

---
---

# ขั้นที่ 2 — สร้าง Telegram Bot

### 2.1 สร้างบอท

1. ติดตั้งแอป **Telegram** บนมือถือ (App Store) แล้วสมัคร
2. ในแอป กดค้นหา พิมพ์ **@BotFather** → เลือกอันที่มีเครื่องหมายถูกสีฟ้า
3. กด **Start** แล้วพิมพ์:
   ```
   /newbot
   ```
4. มันจะถามชื่อบอท → พิมพ์อะไรก็ได้ เช่น `เช็คฝนบางปะกง`
5. มันจะถาม username → **ต้องลงท้ายด้วย `bot`** และห้ามซ้ำกับคนอื่น
   เช่น `bangpakong_rain_2569_bot`
6. BotFather จะตอบกลับมาพร้อมข้อความประมาณนี้:

   ```
   Use this token to access the HTTP API:
   7123456789:AAEhBOweik6ad9r_ZqW4wFvxxxxxxxxxx
   ```

   **คัดลอกบรรทัดยาว ๆ นั้นเก็บไว้ = TOKEN**

   > ⚠️ TOKEN นี้ห้ามให้ใครเห็น ใครมีก็ส่งข้อความในนามบอทคุณได้

### 2.2 หา CHAT_ID

1. ในแอป Telegram ค้นหาชื่อบอทที่เพิ่งสร้าง (ตัว username ที่ลงท้ายด้วย bot)
2. กด **Start** แล้วพิมพ์อะไรก็ได้ส่งไป เช่น `hello`

   > ขั้นนี้สำคัญมาก ถ้าไม่ทักก่อน บอทจะส่งข้อความหาคุณไม่ได้

3. เปิดเบราว์เซอร์บนคอม แล้วพิมพ์ URL นี้ (แทน `<TOKEN>` ด้วยของจริง ไม่ต้องมีวงเล็บ):

   ```
   https://api.telegram.org/bot<TOKEN>/getUpdates
   ```

   ตัวอย่างที่ถูก:
   ```
   https://api.telegram.org/bot7123456789:AAEhBOweik6ad9r_ZqW4wFv.../getUpdates
   ```

4. จะเห็นข้อความ JSON ยาว ๆ ให้หาส่วนที่เขียนว่า:

   ```json
   "chat":{"id":123456789,"first_name":"..."
   ```

   **เลข `123456789` นั่นคือ CHAT_ID**

### ✅ ขั้นที่ 2 เสร็จเมื่อ

มีค่า 2 อย่างจดไว้: **TOKEN** และ **CHAT_ID**

### ถ้าติดปัญหา

| อาการ | แก้ |
|---|---|
| `getUpdates` ขึ้น `{"ok":true,"result":[]}` ว่างเปล่า | ยังไม่ได้ทักบอท ให้ย้อนไปทำข้อ 2.2.2 แล้วโหลดหน้าใหม่ |
| ขึ้น `{"ok":false,"error_code":401}` | TOKEN ผิด คัดลอกมาไม่ครบหรือมีช่องว่างติดมา |
| หา username ที่ไม่ซ้ำไม่ได้ | ใส่ตัวเลขต่อท้าย เช่น `bpk_rain_9981_bot` |

---
---

# ขั้นที่ 3 — ทดสอบบนคอมก่อน

**ทำขั้นนี้ให้ผ่านก่อนเสมอ** อย่าข้ามไปขั้น 4 เพราะถ้ามีปัญหาจะหาสาเหตุยากกว่ามาก

### 3.1 ติดตั้ง Python (ถ้ายังไม่มี)

1. โหลดจาก https://www.python.org/downloads/
2. **ตอนติดตั้ง ต้องติ๊กช่อง `Add python.exe to PATH` ที่หน้าจอแรก**

   > ถ้าลืมติ๊ก ต้องถอนแล้วติดตั้งใหม่ นี่คือปัญหาที่คนเจอบ่อยที่สุด

3. เช็คว่าติดตั้งสำเร็จ — เปิด Command Prompt (กด Win พิมพ์ `cmd`) แล้วพิมพ์:
   ```
   python --version
   ```
   ต้องขึ้นเลขเวอร์ชัน เช่น `Python 3.12.4`

### 3.2 ติดตั้งไลบรารี

```
pip install requests pillow
```

### 3.3 ใส่ TOKEN กับ CHAT_ID

1. เปิดไฟล์ `rain_alert_telegram.py` ด้วย Notepad (คลิกขวา → Open with → Notepad)
2. หาบรรทัดที่ขึ้นต้นด้วย `TELEGRAM_TOKEN` และ `TELEGRAM_CHAT_ID` (อยู่ราวบรรทัดที่ 50)
3. แก้เป็น:

   ```python
   TELEGRAM_TOKEN = os.environ.get("TELEGRAM_TOKEN", "7123456789:AAEhBOweik6...")
   TELEGRAM_CHAT_ID = os.environ.get("TELEGRAM_CHAT_ID", "123456789")
   ```

   > ใส่แทนที่ข้อความ `ใส่_TOKEN_ของบอทตรงนี้` ที่อยู่ในเครื่องหมายคำพูดอันที่สอง
   > **อย่าลบ `os.environ.get(...)` ออก** เพราะขั้นที่ 4 ต้องใช้

4. กด Ctrl+S บันทึก

### 3.4 ทดสอบ

เปิด Command Prompt แล้วพิมพ์ทีละบรรทัด:

```
cd /d D:\AnthropicClaude\Ai\Cladue\Weather
python rain_alert_telegram.py --test
```

**ควรได้ข้อความ "✅ ทดสอบระบบเตือนฝนบางปะกง" เข้า Telegram ภายใน 2-3 วินาที**

ถ้าผ่านแล้ว ลองรันจริง:

```
python rain_alert_telegram.py
```

จะเห็นผลการเช็คบนหน้าจอ ถ้าไม่มีฝนจะขึ้นว่า "ไม่มีอะไรต้องเตือน ไม่ส่งข้อความ" ซึ่งถูกต้อง

อยากบังคับให้ส่งเพื่อดูหน้าตาข้อความ:

```
python rain_alert_telegram.py --force
```

### ✅ ขั้นที่ 3 เสร็จเมื่อ

ได้ข้อความจริงเข้า Telegram และรันคำสั่งปกติแล้วไม่มี error สีแดง

### ถ้าติดปัญหา

| ข้อความที่ขึ้น | สาเหตุ | แก้ |
|---|---|---|
| `'python' is not recognized` | ไม่ได้ Add to PATH | ติดตั้ง Python ใหม่ ติ๊กช่องนั้น |
| `ModuleNotFoundError: requests` | ยังไม่ได้ติดตั้งไลบรารี | `pip install requests pillow` |
| `ส่ง Telegram ไม่สำเร็จ: 401` | TOKEN ผิด | คัดลอก TOKEN ใหม่ ระวังช่องว่าง |
| `ส่ง Telegram ไม่สำเร็จ: 400` | CHAT_ID ผิด | ทักบอทใหม่แล้วเปิด getUpdates อีกครั้ง |
| `!! ยังไม่ได้ตั้งค่า TELEGRAM_TOKEN` | ยังไม่ได้แก้ไฟล์ หรือแก้ผิดที่ | ย้อนไปทำข้อ 3.3 |
| `ดึง Open-Meteo ไม่ได้` | เน็ตมีปัญหา หรือไฟร์วอลล์บล็อก | ลองเปิด URL ในเบราว์เซอร์ดูก่อน |

---
---

# ขั้นที่ 4 — ตั้งให้รันเอง 24 ชม.

เลือกทางใดทางหนึ่ง

## ทาง ก — GitHub Actions (แนะนำ · ไม่ต้องเปิดคอม)

รันบนเซิร์ฟเวอร์ของ GitHub ฟรี ทำงานแม้ปิดคอมหรือไม่อยู่บ้าน

### ก.1 สร้าง repo

1. เข้า https://github.com → กด **+** มุมขวาบน → **New repository**
2. ตั้งชื่อ เช่น `rain-alert`
3. เลือก **Private**

   > เลือก Private เพื่อความปลอดภัย
   > โควตาฟรีของ repo ส่วนตัวคือ 2,000 นาที/เดือน งานนี้ใช้ราว 700-800 นาที/เดือน ยังพอ

4. กด **Create repository**

### ก.2 อัปโหลดไฟล์

1. ในหน้า repo กด **uploading an existing file**
2. ลากไฟล์ `rain_alert_telegram.py` เข้าไปวาง
3. กด **Commit changes**

   > ⚠️ ก่อนอัปโหลด ให้กลับไปแก้ไฟล์ให้ TOKEN เป็นค่าเดิม
   > `os.environ.get("TELEGRAM_TOKEN", "ใส่_TOKEN_ของบอทตรงนี้")`
   > **ห้ามให้ TOKEN จริงติดขึ้น GitHub** เพราะจะใส่ผ่าน Secrets แทน

### ก.3 ใส่ TOKEN แบบปลอดภัย

1. ในหน้า repo กด **Settings** (แท็บบนสุด)
2. เมนูซ้าย → **Secrets and variables** → **Actions**
3. กด **New repository secret** ทำ 2 รอบ:

   | Name | Secret |
   |---|---|
   | `TELEGRAM_TOKEN` | วาง TOKEN ที่ได้จากขั้น 2 |
   | `TELEGRAM_CHAT_ID` | วาง CHAT_ID ที่ได้จากขั้น 2 |

### ก.4 เพิ่มไฟล์ workflow

1. ในหน้า repo กด **Add file** → **Create new file**
2. ในช่องชื่อไฟล์ พิมพ์ให้ตรงเป๊ะ:

   ```
   .github/workflows/rain-alert.yml
   ```

   > พิมพ์เครื่องหมาย `/` แล้ว GitHub จะสร้างโฟลเดอร์ให้เอง
   > ต้องมีจุดนำหน้า `.github` ด้วย

3. เปิดไฟล์ `github-actions/rain-alert.yml` ในโฟลเดอร์งาน คัดลอกเนื้อหาทั้งหมดไปวาง
4. กด **Commit changes**
5. ทำซ้ำอีกรอบสำหรับสรุปประจำวัน — ชื่อไฟล์ `.github/workflows/daily-summary.yml`
   เนื้อหาจากไฟล์ `github-actions/daily-summary.yml`

### ก.5 ทดสอบ

1. กดแท็บ **Actions** ด้านบน
2. เลือก **แจ้งเตือนฝน บางปะกง** จากเมนูซ้าย
3. กด **Run workflow** → **Run workflow** (ปุ่มสีเขียว)
4. รอสัก 1 นาที แล้วกดเข้าไปดูผล ถ้าขึ้นเครื่องหมายถูกสีเขียว = สำเร็จ

### ✅ เสร็จเมื่อ

รันเองแล้วขึ้นถูกสีเขียว และผ่านไป 2-3 ชั่วโมงมีข้อความเข้า Telegram เองตอนมีฝน

### สิ่งที่ต้องรู้เรื่อง GitHub Actions

- **เวลาไม่ตรงเป๊ะ** งานตามตารางของ GitHub มักช้ากว่ากำหนด 5-30 นาที ช่วงที่คนใช้เยอะ
  ไม่ใช่ปัญหาสำหรับการเตือนฝนล่วงหน้า 3 ชั่วโมง
- **หยุดเองถ้าไม่แตะ repo 60 วัน** GitHub จะปิด workflow อัตโนมัติ
  แก้ง่าย ๆ ด้วยการเข้าไปกด Run workflow เองสักครั้งทุก 2 เดือน
- **โควตา** repo ส่วนตัวได้ 2,000 นาที/เดือน งานนี้ใช้ราว 700-800 นาที ถ้าใกล้เต็ม
  ให้ลดเหลือรันทุก 2 ชั่วโมง โดยแก้ cron เป็น `0 */2 * * *`

---

## ทาง ข — Windows Task Scheduler (ง่ายกว่า · ต้องเปิดคอม)

1. กด Win พิมพ์ `Task Scheduler` เปิดขึ้นมา
2. เมนูขวา → **Create Task** (ไม่ใช่ *Create Basic Task*)
3. แท็บ **General**
   - Name: `แจ้งเตือนฝนบางปะกง`
   - ติ๊ก **Run whether user is logged on or not**
4. แท็บ **Triggers** → **New**
   - Begin the task: `On a schedule`
   - เลือก **Daily** · Start เวลา `00:00`
   - ติ๊ก **Repeat task every** เลือก `1 hour`
   - for a duration of: **Indefinitely**
   - กด OK
5. แท็บ **Actions** → **New**
   - Action: `Start a program`
   - Program/script: `python`
   - Add arguments: `rain_alert_telegram.py`
   - Start in: `D:\AnthropicClaude\Ai\Cladue\Weather`
   - กด OK
6. แท็บ **Conditions** → เอาติ๊ก **Start the task only if the computer is on AC power** ออก
7. กด **OK** (อาจถามรหัสผ่าน Windows)

### ✅ เสร็จเมื่อ

คลิกขวาที่ task → **Run** แล้วได้ผลเหมือนตอนรันมือ และผ่านไปสองสามชั่วโมงยังทำงานอยู่

---
---

# ปรับจูนหลังใช้จริง

ใช้ไปสัก 1-2 สัปดาห์ก่อน แล้วค่อยปรับ — แก้ค่าในไฟล์สคริปต์ส่วน CONFIG

| อาการ | แก้ยังไง |
|---|---|
| เตือนบ่อยเกินไป น่ารำคาญ | `RAIN_MM_ALERT = 1.0` และ `PROB_ALERT = 70` |
| เตือนช้า ฝนตกแล้วเพิ่งรู้ | `RAIN_MM_ALERT = 0.2` และ `LOOKAHEAD_HOURS = 4` |
| เตือนซ้ำเรื่องเดิมหลายรอบ | `COOLDOWN_MINUTES = 150` |
| อยากรู้ทุกชั่วโมงแม้ไม่มีฝน | `SEND_WHEN_CLEAR = True` |
| ย้ายไซต์งาน เปลี่ยนพิกัด | แก้ `WX_LAT` / `WX_LON` ในไฟล์ workflow (ทาง ก) หรือ `LAT`/`LON` ในสคริปต์ (ทาง ข) |

**สิ่งที่ควรทำควบคู่:** จดไว้ว่าแต่ละวันระบบทำนายฝนเท่าไร ตกจริงเท่าไร
ทำสัก 2-3 สัปดาห์จะเห็น pattern เฉพาะบางปะกง เช่น "โมเดลมักทำนายฝนบ่ายต่ำกว่าจริง"
แล้วปรับเกณฑ์ให้ตรงความเป็นจริง — นี่คือสิ่งที่ทำให้ระบบแม่นกว่าแอปสำเร็จรูป

---

# สุดทาง

```
เช้า 06:00     Telegram ส่งสรุปอากาศทั้งวัน
ตลอดวัน        Telegram เตือนเมื่อฝนหรือลมแรงจะมาใน 3 ชม.
ก่อนเทคอนกรีต   เปิดแอปบน iPhone ดูช่วงเวลาแห้งต่อเนื่อง 6 ชม.
ทุก 2 เดือน     เข้า GitHub กด Run workflow สักครั้งกัน workflow ถูกปิด
```

**ค่าใช้จ่ายทั้งหมด: 0 บาท**

---

# เช็คลิสต์

- [ ] ขั้น 1.1 อัปโหลด `pwa` ขึ้น Netlify ได้ URL แล้ว
- [ ] ขั้น 1.3 เพิ่มไปยังหน้าจอโฮมบน iPhone แล้ว กดเปิดได้
- [ ] ขั้น 2.1 ได้ TOKEN จาก BotFather แล้ว
- [ ] ขั้น 2.2 ทักบอทแล้ว และได้ CHAT_ID แล้ว
- [ ] ขั้น 3.1 ติดตั้ง Python แล้ว `python --version` ขึ้นเลขเวอร์ชัน
- [ ] ขั้น 3.4 `--test` ส่งข้อความเข้า Telegram สำเร็จ
- [ ] ขั้น 4 ตั้งรันอัตโนมัติแล้ว (GitHub Actions หรือ Task Scheduler)
- [ ] ผ่านไป 1 วันแล้วได้รับข้อความอัตโนมัติจริง

---

*ติดตรงไหนส่งข้อความ error มาได้เลย บอกว่าติดขั้นไหนข้อไหน*
