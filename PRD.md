# PRD — ระบบแจ้งเตือนฝน บางปะกง (rain-alert)

อัปเดตล่าสุด: 23 ก.ย. 2569

## 1. ปัญหาและเป้าหมาย
ไซต์งานก่อสร้างที่บางปะกง (ฉะเชิงเทรา, 13.53, 100.99) ต้องรู้ล่วงหน้าว่าฝน/ลมจะมาเมื่อไหร่
เพื่อวางงานเทคอนกรีต งานสี งานนั่งร้าน/เครน และคลุมวัสดุให้ทัน
เจ้าของงานไม่ได้อยู่หน้างานตลอด จึงต้องการระบบที่ **ทำงานเองโดยไม่ต้องเปิดคอม และไม่ต้องกรอกอะไรเอง**

**เป้าหมาย**
- เตือนฝนที่จะกระทบงานก่อนตก และไม่รบกวนจนคนเลิกอ่าน
- สรุปอากาศทั้งวันให้ถึงมือราว 07:00 เพื่อวางแผนงาน
- วัดความแม่นได้เองอัตโนมัติ เพื่อปรับเกณฑ์จากข้อมูลจริง

## 2. ผู้ใช้
- เจ้าของงาน/ผู้คุมงาน 1 คน รับข้อความผ่าน Telegram (chat id เดียว)

## 3. ขอบเขตฟีเจอร์

### 3.1 การเตือนอัตโนมัติ (`rain_alert_telegram.py --watch 45 --every 5`)
| ทริกเกอร์ | เงื่อนไข |
|---|---|
| ฝน (`rain/heavy/very_heavy`) | โมเดล ≥ 2 ตัวเห็นตรงกัน + ฝน ≥ 1.0 มม./ชม. + โอกาส ≥ 70% + เห็นซ้ำ 2 รอบ |
| เรดาร์ (`radar_now/radar_soon`) | ฝนคลุม ≥ 25% ของรัศมี 6 กม. / คำนวณ ETA จากการเคลื่อนที่ของกลุ่มฝน |
| ลม (`gust`) | กระโชก ≥ 40 กม./ชม. (≥ 60 = อันตราย) |
| พายุ (`cape`) | CAPE ≥ 2500 J/kg **และมีฝนจริงยืนยัน** (เรดาร์หรือโมเดล) |
| ความกดอากาศ (`pressure`) | ลด ≥ 1.5 hPa — เป็นข้อมูลประกอบ เว้นแต่มีเหตุอื่นเตือนอยู่แล้ว |
| อื่น ๆ | ประกาศเตือนภัยกรมอุตุฯ, ฝนหนักตรงน้ำขึ้นสูง, ความร้อน (รู้สึก ≥ 41°C) |

กันรบกวน: cooldown 120 นาที (ยกเว้นรุนแรงขึ้น), งดรบกวน 22:00–05:00 (ผ่านได้เฉพาะระดับ ≥ 5)

### 3.2 สรุปเช้า (`--daily`)
ภาพรวม · แดด/เมฆ · อุณหภูมิ · ช่วงฝน · ลมและทิศ · UV · พระอาทิตย์ขึ้น-ตก · คำแนะนำงานตามบริบท
- สรุปเฉพาะชั่วโมงที่เหลือของวัน
- workflow เริ่ม 05:17 แล้วรอส่ง 06:58; ถ้ารันช้าหลัง 07:30 ขึ้นป้าย "ส่งช้า"
- ลองดึงข้อมูล/ส่งซ้ำ 3 ครั้ง

### 3.3 วัดผลและบันทึก
- `alert_log.csv` — ทุกครั้งที่ระบบคิดจะเตือน (ทริกเกอร์ทั้งหมด, % เรดาร์, ส่งจริงหรือไม่)
- `radar_watch.csv` — ค่าที่วัดได้ทุกรอบ ไม่ว่าจะเตือนหรือไม่
- `analyze_alerts.py` — เทียบกับฝนจริงจาก Open-Meteo อัตโนมัติ: ความแม่น, แยกทริกเกอร์, อัตราจับฝน, baseline
- `rain_times.txt` — (ทางเลือก) จดฝนจริงเองเพื่อจูนเรดาร์ให้แม่นกว่า

### 3.4 เครื่องมือเสริม
- `rain_bot_interactive.py` — บอทตอบคำถาม (รันบนคอม) ใช้สมองเรดาร์ตัวเดียวกับตัวเตือน
- `cloudflare-worker/worker.js` — บอทบน Cloudflare (ยังไม่ได้ตั้ง webhook = ไม่ทำงานจริง)
- `pwa/`, `chachoengsao_*.html` — แดชบอร์ดดูข้อมูล (แสดงผล Open-Meteo ดิบ ไม่ตัดสินใจเตือน)

## 4. แหล่งข้อมูล
Open-Meteo (ECMWF / GFS / ICON), TMD WRF (ต้องใช้ TMD_TOKEN), RainViewer (เรดาร์ ซูม 7),
ประกาศเตือนภัยกรมอุตุฯ, Open-Meteo Marine (น้ำขึ้นน้ำลง)

## 5. ข้อจำกัดและข้อบังคับ
- รันบน GitHub Actions — **repo ต้องเป็น public** (private โควตา 2,000 นาที/เดือน ใช้จริง ~9,000)
- ความลับอยู่ใน GitHub Secrets และไฟล์ในเครื่องที่ `.gitignore` กันไว้ ห้ามฝังในโค้ด
- ตัวตั้งเวลาของ GitHub ไม่แน่นอน อาจช้าหรือข้ามรอบ

## 6. ตัวชี้วัดความสำเร็จ
| ตัวชี้วัด | เป้า |
|---|---|
| ส่งแล้วฝนตกจริง | ≥ 75% และสูงกว่า baseline ≥ 15 จุด |
| ฝนจริงที่จับได้ (ช่วง 05–22) | ≥ 75% |
| ข้อความเตือน/วัน | ≤ 3–4 |
| สรุปเช้าถึงมือ | 06:30–07:30 |
| รอบเช็คจริง/วัน (`radar_watch`) | ≥ 120 |

## 7. นอกขอบเขต (ตอนนี้)
- รายงานตามพิกัดที่ส่งเข้ามาใน Telegram (ออกแบบไว้แล้ว ยังไม่ทำ)
- เตือนหลายไซต์พร้อมกัน
