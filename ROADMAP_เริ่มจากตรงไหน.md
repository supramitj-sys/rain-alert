# ROADMAP — เริ่มจากตรงไหน จนถึงสุดทาง

---

# ส่วน A — งานอากาศ

## คุณอยู่ตรงไหนแล้ว

```
[✓] เว็บแอปเสร็จแล้ว          ← ไฟล์อยู่ในโฟลเดอร์ เปิดใช้ได้เลย
[✓] PWA เตรียมไว้แล้ว         ← รอแค่อัปโหลด
[✓] สคริปต์ Telegram เขียนแล้ว ← รอใส่ token
[✓] สรุปเช้า 6:00 ทำงานอยู่    ← ในแอป Claude
[ ] ยังไม่ได้ทำ: 3 ขั้นข้างล่าง
```

---

## ขั้นที่ 1 — เอา PWA ขึ้น iPhone

**เวลา:** 15 นาที · **ยากแค่ไหน:** ง่ายมาก ไม่ต้องเขียนโค้ด

1. เข้า https://app.netlify.com (คุณมีบัญชีแล้ว)
2. **Sites → Add new site → Deploy manually**
3. ลากโฟลเดอร์ `pwa` ทั้งโฟลเดอร์เข้าไปวางในกรอบ
4. รอ 10-20 วินาที จะได้ URL แบบ `https://xxx.netlify.app`
5. เปลี่ยนชื่อให้จำง่าย: **Site configuration → Change site name** → `bangpakong-rain`
6. เปิด URL นั้นด้วย **Safari บน iPhone** (Chrome ทำไม่ได้)
7. กดปุ่มแชร์ → **"เพิ่มไปยังหน้าจอโฮม"**

**เสร็จเมื่อ:** มีไอคอนเมฆฝนบนหน้าจอ iPhone กดแล้วเปิดเต็มจอ ไม่มีแถบ Safari

**ถ้าติด:** ถ้ากดปุ่มตำแหน่งปัจจุบันแล้วไม่ขึ้น ให้ไป Settings → Safari → Location → Allow

---

## ขั้นที่ 2 — ตั้ง Telegram Bot

**เวลา:** 10 นาที · **ยากแค่ไหน:** ง่าย แต่ต้องคัดลอกรหัสให้ถูก

1. ติดตั้งแอป Telegram แล้วค้นหา **@BotFather**
2. พิมพ์ `/newbot` → ตั้งชื่อบอท → ตั้ง username (ต้องลงท้ายด้วย `bot`)
3. BotFather ส่ง **TOKEN** มาให้ หน้าตาแบบ `7123456789:AAExxxxxxxx` → **คัดลอกเก็บไว้**
4. ค้นหาชื่อบอทที่เพิ่งสร้าง กด **Start** แล้วพิมพ์อะไรก็ได้ส่งไป 1 ครั้ง
5. เปิดในเบราว์เซอร์: `https://api.telegram.org/bot<TOKEN>/getUpdates`
   (แทน `<TOKEN>` ด้วยของจริง ไม่ต้องมีวงเล็บ)
6. หาเลขในส่วน `"chat":{"id":123456789` → นั่นคือ **CHAT_ID**

**เสร็จเมื่อ:** มี TOKEN กับ CHAT_ID เก็บไว้ทั้งสองค่า

---

## ขั้นที่ 3 — ทดสอบสคริปต์บนคอมก่อน

**เวลา:** 10 นาที · **ยากแค่ไหน:** ปานกลาง ต้องใช้ Command Prompt

1. ติดตั้ง Python จาก python.org ถ้ายังไม่มี (ตอนติดตั้ง **ติ๊ก "Add to PATH"** ด้วย)
2. เปิด Command Prompt พิมพ์:
   ```
   pip install requests pillow
   ```
3. เปิดไฟล์ `rain_alert_telegram.py` ด้วย Notepad แก้ 2 บรรทัด:
   ```python
   TELEGRAM_TOKEN = "ใส่ TOKEN ที่ได้จากขั้นที่ 2"
   TELEGRAM_CHAT_ID = "ใส่ CHAT_ID ที่ได้จากขั้นที่ 2"
   ```
4. ทดสอบการเชื่อมต่อ:
   ```
   cd D:\AnthropicClaude\Ai\Cladue\Weather
   python rain_alert_telegram.py --test
   ```
5. ถ้าข้อความเข้า Telegram = ผ่าน แล้วลองรันจริง:
   ```
   python rain_alert_telegram.py
   ```

**เสร็จเมื่อ:** ได้ข้อความทดสอบใน Telegram และรันจริงแล้วไม่มี error

**ถ้าติด:**
- `'python' is not recognized` → ยังไม่ได้ Add to PATH ให้ติดตั้ง Python ใหม่แล้วติ๊กช่องนั้น
- `ส่ง Telegram ไม่สำเร็จ: 400` → CHAT_ID ผิด ลองทักบอทใหม่แล้ว getUpdates อีกครั้ง
- `ส่ง Telegram ไม่สำเร็จ: 401` → TOKEN ผิด

---

## ขั้นที่ 4 — ให้รันเองอัตโนมัติ

**เวลา:** 15 นาที · **เลือกทางใดทางหนึ่ง**

### ทาง ก — Windows Task Scheduler (ง่ายกว่า แต่ต้องเปิดคอม)

1. กด Win พิมพ์ `Task Scheduler` เปิดขึ้นมา
2. คลิกขวาที่ **Task Scheduler Library → Create Task** (ไม่ใช่ Basic Task)
3. **General:** ตั้งชื่อ → ติ๊ก *Run whether user is logged on or not*
4. **Triggers → New:** เลือก Daily เวลา 00:00 → ติ๊ก *Repeat task every 1 hour* → *for a duration of: Indefinitely*
5. **Actions → New:** Program = `python` · Arguments = `rain_alert_telegram.py` · Start in = `D:\AnthropicClaude\Ai\Cladue\Weather`
6. **Conditions:** เอาติ๊ก *Start only if on AC power* ออก
7. กด OK

### ทาง ข — GitHub Actions (ไม่ต้องเปิดคอม แนะนำ)

รันบนเซิร์ฟเวอร์ GitHub ฟรี ทำงาน 24 ชม.จริงแม้ปิดคอม
ขั้นตอนซับซ้อนกว่านิดหน่อย — **บอกผมได้ ผมเขียนไฟล์ workflow ให้พร้อมใช้**

**เสร็จเมื่อ:** ผ่านไป 2-3 ชั่วโมงแล้วมีข้อความเข้า Telegram เองโดยไม่ต้องสั่ง

---

## ขั้นที่ 5 — ปรับจูนให้เข้ากับตัวเอง

**เวลา:** ใช้จริง 2 สัปดาห์แล้วค่อยปรับ

เปิดไฟล์สคริปต์ แก้ค่าในส่วน CONFIG:

| อาการ | แก้ยังไง |
|---|---|
| เตือนบ่อยเกินไป น่ารำคาญ | `RAIN_MM_ALERT` เป็น 1.0 · `PROB_ALERT` เป็น 70 |
| เตือนช้า ฝนตกแล้วเพิ่งรู้ | `RAIN_MM_ALERT` เป็น 0.2 · `LOOKAHEAD_HOURS` เป็น 4 |
| เตือนซ้ำเรื่องเดิม | เพิ่ม `COOLDOWN_MINUTES` เป็น 120 |
| อยากได้ข้อความแม้ไม่มีฝน | `SEND_WHEN_CLEAR = True` |

**สิ่งที่ควรทำควบคู่:** จดลงสมุดหรือ Sheet ว่าแต่ละวันระบบทำนายไว้เท่าไร ตกจริงเท่าไร
ทำสัก 2-3 สัปดาห์แล้วคุณจะเห็น pattern เฉพาะพื้นที่บางปะกง ซึ่งไม่มีในโมเดลไหน
เช่น "โมเดลมักทำนายฝนบ่ายต่ำกว่าจริง" แล้วปรับเกณฑ์ให้ตรงกับความเป็นจริง

---

## สุดทางของงานอากาศ

```
เช้า 06:00  → Claude สรุปพยากรณ์ทั้งวันให้
ตลอดวัน     → Telegram เตือนเมื่อฝน/ลมจะมา
ก่อนเทคอนกรีต → เปิด PWA ดูช่วงเวลาแห้งต่อเนื่อง
ทุกเดือน     → ปรับเกณฑ์ตามที่ใช้จริง
```

---
---

# ส่วน B — รวมทุกระบบให้อยู่จุดเดียว

## สิ่งที่คุณมีอยู่และอยากเพิ่ม

| ระบบ | สถานะ | ช่องทาง |
|---|---|---|
| รายรับ-รายจ่าย | ใช้อยู่ | LINE OA + Make → Sheet |
| Man-day | ใช้อยู่ | LINE → Sheet |
| อากาศ | เพิ่งทำ | Claude + Telegram |
| WealthX | ใช้อยู่ | Claude scheduled task |
| บันทึกความจำ (วันหยุด/หาหมอ/เที่ยว) | อยากทำ | — |
| บันทึกเลขบัญชี | อยากทำ | — |

---

## หลักการออกแบบ: แยก 4 ชั้น อย่าปนกัน

ปัญหาที่คนมักเจอเวลารวมระบบคือ **รวมทุกอย่างเข้าด้วยกันจนแก้อะไรไม่ได้เลย**
ทางที่ยั่งยืนคือแยกเป็น 4 ชั้น แต่ละชั้นเปลี่ยนได้โดยไม่พังชั้นอื่น

```
ชั้นที่ 1  รับข้อมูลเข้า     LINE OA (พิมพ์สั่ง)
ชั้นที่ 2  ตัวกลางประมวลผล   Make (Router แยกตามคำสั่ง)
ชั้นที่ 3  ที่เก็บข้อมูล      Google Sheet เดียว หลายชีต
ชั้นที่ 4  แสดงผล + แจ้งเตือน Dashboard (ดู) + Telegram (เตือน)
```

---

## ทำไมต้องใช้ทั้ง LINE และ Telegram

ผมเช็คโควตามาแล้ว ตัวเลขบังคับให้ต้องแยกหน้าที่:

**LINE OA แผนฟรีในไทย = 200 ข้อความ/เดือน** แต่มีข้อยกเว้นสำคัญ:

- **ข้อความตอบกลับ (reply) = ฟรี ไม่จำกัด** — คือตอนคุณพิมพ์สั่งแล้วบอทตอบ
- **ข้อความที่บอทส่งมาเอง (push) = นับโควตา** — คือการแจ้งเตือน

ลองคำนวณถ้าใช้ LINE แจ้งเตือนอย่างเดียว:

```
เตือนฝน       3 ครั้ง/วัน × 30 = 90
สรุปเช้า       1 ครั้ง/วัน × 30 = 30
เตือนนัดหมาย   2 ครั้ง/วัน × 30 = 60
                        รวม = 180/200  ← เต็มโควตาแทบพอดี
```

พอเพิ่มงานใหม่อีกนิดเดียวก็ทะลุ ต้องจ่าย 599 บาท/เดือน

**ทางแก้ที่ไม่เสียเงินเลย — แยกตามทิศทาง:**

| ทิศทาง | ใช้อะไร | เหตุผล |
|---|---|---|
| **คุณพิมพ์เข้าไป** (บันทึกรายจ่าย, man-day, โน้ต) | **LINE** | reply ฟรีไม่จำกัด และ LINE คุณเปิดอยู่แล้วทั้งวัน |
| **ระบบเตือนคุณ** (ฝน, นัดหมาย, สรุป) | **Telegram** | push ฟรีไม่จำกัด ไม่มีเพดาน |

ดูเหมือน 2 แอป แต่จริง ๆ แล้วคุณจะไม่สับสน เพราะ **LINE = ที่พิมพ์ · Telegram = ที่อ่าน**
แยกกันชัดเจนตามธรรมชาติการใช้งาน

> ถ้าอยากเหลือแอปเดียวจริง ๆ ให้ย้ายทุกอย่างไป Telegram (ฟรีหมด ไม่มีเพดานทั้งสองทาง)
> แต่ต้องรื้อ flow รายจ่ายกับ man-day ที่ใช้ได้ดีอยู่แล้วมาทำใหม่ ซึ่งผมไม่แนะนำถ้าไม่จำเป็น

---

## โครงสร้าง Google Sheet: ไฟล์เดียว หลายชีต

อย่าแยกเป็นหลายไฟล์ เพราะจะทำ Dashboard รวมยาก

```
📗 ระบบงานส่วนตัว.xlsx  (ไฟล์เดียว)
   ├─ 00_Config      ตั้งค่ากลาง — ชื่อโครงการ, หมวดรายจ่าย, อัตราค่าแรง
   ├─ 01_รายรับจ่าย   วันที่ | ประเภท | หมวด | จำนวนเงิน | โครงการ | หมายเหตุ
   ├─ 02_Manday      วันที่ | โครงการ | ประเภทช่าง | จำนวนคน | งานที่ทำ
   ├─ 03_บันทึก      วันที่ | ประเภท | เรื่อง | วันที่เกี่ยวข้อง | สถานะ
   ├─ 04_อากาศ       วันที่ | ทำนาย(มม.) | ตกจริง(มม.) | หยุดงานไหม
   ├─ 05_ความก้าวหน้า วันที่ | โครงการ | งวดงาน | %แผน | %จริง
   └─ 99_Log         บันทึกทุกข้อความที่ส่งเข้ามา (ไว้ debug)
```

**ทำไมต้องมี `99_Log`:** เวลาบอทตีความคำสั่งผิด คุณจะย้อนดูได้ว่าส่งอะไรเข้าไปจริง ๆ
โดยไม่ต้องเดา — ประหยัดเวลามากตอนแก้ปัญหา

**ทำไมต้องมี `00_Config`:** เวลาเพิ่มโครงการใหม่หรือเปลี่ยนอัตราค่าแรง
แก้ที่เดียวแล้วทุกชีตใช้ตาม ไม่ต้องไล่แก้สูตรทีละที่

**ทำไมต้องมี `04_อากาศ`:** นี่คือชีตที่ทำให้ระบบฉลาดขึ้นเรื่อย ๆ
บันทึกว่าทำนายเท่าไร ตกจริงเท่าไร ผ่านไป 3 เดือนคุณจะรู้ว่าโมเดลเอียงไปทางไหน
และใช้เป็นหลักฐานประกอบเวลาขอขยายเวลาก่อสร้าง (แต่ต้องขอหนังสือรับรองจากกรมอุตุฯ ควบคู่ด้วย)

---

## Make: ใช้ Router ตัวเดียว ไม่ใช่หลาย Scenario

นี่คือหัวใจของการรวมให้อยู่จุดเดียว

```
LINE Webhook (รับทุกข้อความ)
        ↓
   บันทึกลง 99_Log ก่อนเสมอ
        ↓
   Router แยกตามคำขึ้นต้น
        ├─ ขึ้นต้น "จ่าย" หรือ "รับ"  → เขียนลง 01_รายรับจ่าย
        ├─ ขึ้นต้น "คน" หรือ "manday" → เขียนลง 02_Manday
        ├─ ขึ้นต้น "จำ" หรือ "นัด"    → เขียนลง 03_บันทึก
        ├─ ขึ้นต้น "งาน"              → เขียนลง 05_ความก้าวหน้า
        ├─ ขึ้นต้น "สรุป"             → อ่าน Sheet แล้วตอบกลับ
        └─ ไม่ตรงอะไรเลย              → ตอบวิธีใช้
        ↓
   ตอบกลับยืนยัน (reply = ฟรี)
```

**รูปแบบคำสั่งที่แนะนำ** — ออกแบบให้พิมพ์เร็วบนมือถือ:

```
จ่าย 2500 ค่าปูน โครงการA
รับ 150000 งวด2 โครงการA
คน 12 ช่างปูน โครงการA ก่ออิฐชั้น2
จำ 15/8 หาหมอ รพ.สมิติเวช 10:00
นัด 20/8 ประชุมเจ้าของโครงการ
งาน โครงการA งวด3 65%
สรุป เดือนนี้
สรุป โครงการA
```

**ข้อดีของการใช้ Router ตัวเดียว:**
- LINE OA มี webhook URL ได้เพียง 1 อัน — ถ้าแยกหลาย scenario จะชนกัน
- เพิ่มงานใหม่ = เพิ่มกิ่งใน Router ไม่ต้องสร้าง scenario ใหม่
- โควตา Make ฟรี 1,000 ops/เดือน ใช้ร่วมกันได้คุ้มกว่า

**ระวังเรื่องโควตา Make:** แผนฟรี 1,000 operations/เดือน
1 ข้อความที่ส่งเข้ามา ≈ 3-5 ops (webhook + router + เขียน sheet + reply)
แปลว่ารับได้ราว **200-300 ข้อความ/เดือน** = วันละ 7-10 ครั้ง
ถ้าไม่พอ ให้ย้ายไป **Google Apps Script** ซึ่งฟรีไม่จำกัดและอยู่ใน Google Sheet อยู่แล้ว

---

## Dashboard เดียว แยกแท็บตามงาน

ทำเป็น PWA เหมือนแอปอากาศ อัปขึ้น Netlify แล้ว Add to Home Screen

```
┌─────────────────────────────────┐
│  ระบบงาน — [ชื่อโครงการ ▼]      │
├─────────────────────────────────┤
│ 💰 การเงิน │ 👷 คน │ 🌧️ อากาศ │ 📝 บันทึก │
├─────────────────────────────────┤
│                                 │
│   เนื้อหาตามแท็บที่เลือก          │
│                                 │
└─────────────────────────────────┘
```

**วิธีให้เว็บอ่าน Google Sheet โดยไม่ต้องทำ backend:**
ใน Google Sheet เลือก **File → Share → Publish to web → เลือกชีต → CSV**
จะได้ URL ที่ดึงด้วย JavaScript ได้ตรง ๆ

⚠️ **สำคัญ:** วิธีนี้ทำให้ข้อมูลชีตนั้น**ใครมี URL ก็เปิดดูได้**
ใช้ได้กับชีตอากาศหรือความก้าวหน้า **แต่ห้ามใช้กับชีตที่มีข้อมูลการเงินหรือเลขบัญชีเด็ดขาด**
สำหรับชีตที่เป็นความลับ ให้ใช้ Google Apps Script Web App ที่ตรวจสอบสิทธิ์แทน

---

## ⚠️ เรื่องเลขบัญชี — ขอเตือนตรง ๆ

คุณบอกว่าอยากเก็บเลขบัญชีด้วย ตรงนี้ผมต้องเตือนก่อน

**อย่าเก็บเลขบัญชีเต็ม ๆ ในระบบที่เข้าถึงผ่านแชทบอท** เหตุผล:

1. ข้อความใน LINE/Telegram ถูกเก็บไว้บนเซิร์ฟเวอร์ผู้ให้บริการ
2. ถ้ามือถือหาย คนที่ได้เครื่องไปเปิดแชทก็เห็นทันที
3. Make และ Google Sheet ถ้าตั้งค่าแชร์ผิดพลาดครั้งเดียว ข้อมูลรั่วทั้งหมด
4. ถ้าเป็นเลขบัญชีของลูกค้าหรือผู้รับเหมา จะเข้าข่าย **PDPA** ซึ่งมีโทษทางกฎหมาย

**ทางที่ปลอดภัยกว่า:**

| เก็บอะไร | เก็บที่ไหน |
|---|---|
| เลขบัญชีเต็ม + รหัสผ่าน | **Password manager** (Bitwarden ฟรี, 1Password) |
| ชื่อธนาคาร + 4 ตัวท้าย + ชื่อบัญชี | Google Sheet ได้ (พอให้จำได้ว่าอันไหน) |
| สลิปโอนเงิน | Google Drive โฟลเดอร์ที่ไม่แชร์ |

ในชีตเก็บแค่ `กสิกร ...4521 ร้านวัสดุ ก.` ก็พอให้คุณรู้ว่าหมายถึงบัญชีไหน
ส่วนเลขเต็มอยู่ใน password manager ซึ่งเข้ารหัสและมี 2FA

---

## ลำดับที่ควรทำ

**อย่าทำพร้อมกันทั้งหมด** จะพังทั้งระบบและหาสาเหตุไม่เจอ

### เฟส 1 — ทำให้อากาศจบก่อน (สัปดาห์นี้)
ทำตามส่วน A ขั้น 1-4 ให้เสร็จ
**เหตุผล:** เป็นระบบเดี่ยว ไม่กระทบของเดิม ได้ฝึกมือกับ Telegram + Netlify ซึ่งจะใช้ต่อในเฟสถัดไป

### เฟส 2 — จัดบ้าน Google Sheet (สัปดาห์หน้า)
รวมชีตรายจ่ายกับ man-day เข้าไฟล์เดียว เพิ่ม `00_Config` และ `99_Log`
**อย่าเพิ่งแตะ Make** แค่จัดโครงสร้างข้อมูลก่อน แล้วแก้ปลายทางของ scenario เดิมให้ชี้มาที่ไฟล์ใหม่
**เกณฑ์ว่าเสร็จ:** ส่งข้อความเดิมเข้า LINE แล้วยังลงชีตถูกเหมือนเดิม

### เฟส 3 — รวม Make เป็น Router เดียว (2 สัปดาห์)
รวม scenario รายจ่าย + man-day เข้าเป็นอันเดียวที่มี Router
**ทำทีละกิ่ง ทดสอบทีละกิ่ง** อย่ารวมรวดเดียว
**เกณฑ์ว่าเสร็จ:** พิมพ์คำสั่งทั้ง 2 แบบเดิมแล้วยังทำงานถูก และมี Log บันทึกครบ

### เฟส 4 — เพิ่มงานใหม่ทีละอย่าง (เดือนถัดไป)
เพิ่มกิ่ง `จำ` และ `นัด` ลง Router → เก็บลง `03_บันทึก`
ตั้ง Make scenario แยกอีกอันที่รันทุกเช้า อ่านชีตบันทึก ถ้ามีนัดวันนี้ให้ยิงเข้า Telegram
**เกณฑ์ว่าเสร็จ:** พิมพ์ `จำ 15/8 หาหมอ` แล้ววันที่ 15 ได้แจ้งเตือนจริง

### เฟส 5 — Dashboard รวม (เมื่อข้อมูลเริ่มเยอะพอ)
ทำ PWA แท็บเดียวกับแอปอากาศ อ่านจากชีตต่าง ๆ
**อย่าทำก่อนหน้านี้** เพราะยังไม่รู้ว่าจะดูอะไรจริง ๆ จนกว่าจะใช้ไปสักพัก

---

## สุดทางของทั้งระบบ

```
พิมพ์ใน LINE  →  "จ่าย 2500 ค่าปูน โครงการA"
                 "คน 12 ช่างปูน โครงการA"
                 "จำ 15/8 หาหมอ 10:00"
                        ↓
              Make Router แยกตามคำสั่ง
                        ↓
              Google Sheet ไฟล์เดียว หลายชีต
                        ↓
        ┌───────────────┴───────────────┐
   Telegram เตือน                 PWA Dashboard
   • ฝนจะมา                       • สรุปการเงินรายเดือน
   • นัดหมายวันนี้                  • man-day สะสม
   • สรุปเช้า                      • ความก้าวหน้าเทียบแผน
                                   • ประวัติอากาศ
```

**ทุกอย่างในนี้ฟรีหมด** ยกเว้นถ้าโควตา Make ไม่พอ ซึ่งย้ายไป Google Apps Script ได้ฟรีเช่นกัน

---

## ถ้าจะให้ผมช่วยต่อ

บอกได้เลยว่าอยากเริ่มอันไหน ผมทำให้ได้:

- เขียนไฟล์ GitHub Actions workflow ให้สคริปต์ Telegram รันเองโดยไม่ต้องเปิดคอม
- ออกแบบโครงสร้าง Google Sheet พร้อมสูตรและ Config ให้ครบ
- เขียน Google Apps Script แทน Make ถ้าโควตาไม่พอ
- ทำ PWA Dashboard รวมทุกงาน
- เขียน prompt สำหรับ Make Router ให้ตีความคำสั่งภาษาไทยได้แม่นขึ้น
